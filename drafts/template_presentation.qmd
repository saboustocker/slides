---
title: "HSG Corporate Template for Quarto"
author: Sabou Rani Stocker
date: 2025-11-10
subtitle: All the slides and styles 
format:
  revealjs:  
    theme: [../styles/hsg_slides.scss]
    slide-number: c
title-slide-attributes: 
  data-background-color: "#0a5f2d"

project: "collection"
bibliography: ../bibliography.bib
---

:::{.section-header-bg}
# Section Title
:::{.subtitle-p}
I am an optional subtitle
:::
:::

:::{.quote-slide-bg}
##
It is possible to commit no mistakes and still lose. That is not a weakness; that is life.

:::{.source-p}
Jean-Luc Picard
:::

:::

## {.callout-slide-p}
This is a very interesting fancy callout! 


## Normal Slide with content
This is normal content.

- bullet point 1
- bullet point 2
- bullet point 3

## Normal Slide with Subtitles
### This is a subtitle
Here, we have some text. Penguins are the cutest animals in the universe! 

### This is another subtitle
Here we have some more text!! 

:::{.center}
## Centered Slide
This slide has the content centered.
:::

## Slide with Quotes
This slides has some quote options on it. 

- This bullet point is a statement corroborated by some authors

  :::{.bib-p}
  @abdurahmanDeepLearningApproach2024; @abramovitchFactorCognitiveDysfunction2021
  :::

- This bullet point has an inline-citation [@abramovitchFactorCognitiveDysfunction2021]

:::{.quote-p}
And this is a quote.

:::{.bib-quote-p}
@abdurahmanDeepLearningApproach2024, p. 34
:::
:::

:::{.image-slide-bg}
## Image Slide
This slide is when you have light images because they don't always work with dark background.

![I am some caption for this interesting image. I can also have a source [@abdurahmanDeepLearningApproach2024].](../images/uncannyvalley.png){style="max-width: 100%; height: auto;"}

:::

## Another Image Slide
Of course, you can still use the regular background. 

![This is how an image on the regular slides looks like.](../images/uncannyvalley.png){style="max-width: 100%; height: auto;"}

:::{.question-slide-bg .center}
## 

:::{.column #vcenter width="30%"}

![](../styles/diskussion.svg){style="max-width: 70%; height: auto;"}

:::

:::{.column #vcenter width="70%" .center}
:::{.question-slide-p}
This is a slide where you can ask an important question?
:::
:::

:::

## Slide with Tabsets

::: {.panel-tabset}

### Tab One
Tabsets can be helpful to have secondary information on hand if needed.

### Tab Two
You can for example have a graph in the second tab. 

:::{.center-align}
![](../images/uncannyvalley.png){width="70%"}
:::

### Tab Three

Or some code that shows how you specified your model. 

```{r}
a <- 1
b <- 4
c <- a+b
d <- c-2

c+d
```

:::

## Slide with Code 
You can also add Code to your slide and highlight certain lines of code:

```{.python code-line-numbers="1-2,4"}
import random

moods = ["😄 Happy", "😎 Cool", "🤔 Curious", "😴 Sleepy", "🤯 Mind-blown", "🐍 Pythonic"]
today_mood = random.choice(moods)

print(f"Your mood today is: {today_mood}")
```

## Slide with long and Complex Code 
You can also add a slide with long and complex code, and highlight the relevant lines: 

```{.python code-line-numbers="6-7|9|19-23|31|35|90"}
import requests
from bs4 import BeautifulSoup
import pandas as pd


base_url = "https://www.psychologie.uzh.ch"
overview_url = base_url + "/de/bereiche/uebersicht.html"

response = requests.get(overview_url)
soup = BeautifulSoup(response.text,"html.parser")



## Level 1 subpages
subpage_links = []
all_data = []


for link in soup.find_all("a",href=True):
    href = link["href"]
    if href.startswith("/de/bereiche/") and href.endswith(".html"):
        subpage_links.append(base_url+href)
        print(base_url+href)

for subpage_url in subpage_links:
    
    print(f"\nScraping {subpage_url}")
    response = requests.get(subpage_url)
    sub_soup = BeautifulSoup(response.text,"html.parser")

    chair = sub_soup.find("h1") ## e.g. Entwicklungspsychologie
    chair_name = chair.get_text(strip=True)
    print(chair_name)

    tables = sub_soup.find_all("table", class_="basic") ## e.g. "Entwicklungspsychologie: Kinder- und Säuglingsalter"

    for i, table in enumerate(tables):
        print(f"\nTable {i+1} in {subpage_url}")
        table_data = []
        table_data.append(chair_name)
        rows = table.find_all("tr")

        for i, row in enumerate(rows):
        
            cells = row.find_all(["th","td"])
            length = len(cells)
            print(f"\nlen: {length}")

            if length == 2:
                title = cells[0].get_text(strip=True) if len(cells) > 0 else ""
                table_data.append(title)
                link = cells[1].find("a",href=True)
                table_data.append(link)
                print(f"\niteration: {i}")
                print("Length = 2")
                print(f"\nTitle: {title}")
                print(f"\nLink: {link}")
                if link:
                    full_description = []
                    subsubpage_link = base_url + link["href"]
                    subresponse = requests.get(subsubpage_link)
                    subsub_soup = BeautifulSoup(subresponse.text, "html.parser")
                    description = subsub_soup.find("section", class_="ContentArea")
                    
                    if description:
                        section_text = description.get_text(strip=True, separator=" ")
                        full_description.append(section_text)
                        
                    else:
                        print("No section found")
                    description_text = " ".join(full_description)
                    table_data.append(description_text)
                    print(f"\ndescription: {description_text}")
            elif length == 1 and i == 1:
                print(f"\niteration: {i}")
                print("Length = 1")
                name = cells[0].get_text(strip=True) if len(cells) > 0 else ""
                print(f"\nName: {name}")
                table_data.append(name)
            elif length == 1 and i == 2:
                print(f"\niteration: {i}")
                print("Length = 1")
                position = cells[0].get_text(strip=True) if len(cells) > 0 else ""
                print(f"\nPosition: {position}")
                table_data.append(position)
            
            print(table_data)
            all_data.append(table_data)

df = pd.DataFrame(all_data)
df.to_csv("beautifulsoup.csv",index=False,sep=";",quoting=1,encoding="utf-8-sig")
```

## Page with an Iframe
You can also add external websites to your Slides: 

<div style="text-align: center;">
<iframe src="https://saboustocker.shinyapps.io/webapp_analysis/" width="900" height="600" frameborder="0"></iframe>
</div>


# Bibliography {.scrollable}